<%= stylesheet_link_tag "users", :media => "all" %>

<h2>フォロワー</h2>
<div class="container">
    <div class="row">
        <% @user.followers.each do |follower| %>
            <%= render partial: "users/followman", locals: { t: follower } %>
        <% end %>
    </div>
</div>